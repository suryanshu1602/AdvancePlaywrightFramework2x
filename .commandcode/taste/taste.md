# Taste

- Communicates in terse, minimal prompts (e.g., "re run") and expects the agent to infer the full task from context. Confidence: 0.6
- Wants code changes/fixes verified by re-running the compiler or checks (e.g., `npx tsc --noEmit`) after edits. Confidence: 0.7
